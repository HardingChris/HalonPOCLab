import { is_trusted_user } from "modules/authentication";

echo "Executing Auth Hook";

$transactionid = $transaction["id"];
$username      = $arguments["username"];
$password      = $arguments["password"];

if(is_trusted_user($username, $password)) Accept();

Reject("Invalid credentials ($transactionid)");